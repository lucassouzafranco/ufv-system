import React from "react";
import { Fragment } from "react";

const Card = (variant) => {
  return(
    <Fragment>
      {variant === 'confirm' && (
        <h2>olaaaa</h2>
      )}
    </Fragment>
  )
}

export default Card;