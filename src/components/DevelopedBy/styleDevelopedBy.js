import styled from "styled-components";

export const Credits = styled.p`
  color: #fff;
  position: absolute;
  font-size: 10pt;
  bottom: 1px;
  font-weight: 600;
  @media (max-width: 414px) {
    color: #111;
  }
`